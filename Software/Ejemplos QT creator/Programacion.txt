En archivo .pro
LIBS += -L/usr/local/lib -lwiringPi

//incluimos wirinPi

en archivo de cabecera .h
#include <wirinPi.h>
#include <pcf8591.h>
//incluimos libreria wiringPi y pcf8591

Ahora ya empesamos a programar lo que seria la parte grafica .cpp

#include "mainwindow.h"
#include "ui_mainwindow.h"
#define Direccion 0x48
#define LEC1 5
#define AN1 1
#define AN2 2
#define AN3 3
#define BASE 120
MainWindow::MainWindow (QWidget *parent):
   QMainWindow(parent),
   ui (new Ui::MainWindow)
  {
   ui-> setup(this);
   wiringPiSetup();
   pcf8591Setup(BASE,Direccion); 
  }
 MainWindow::~MainWindow()
{ 
  delete ui;
}
void MainWindow:: label1 (){
  int valor;
  float tensionbateria;
  valor AnalogRead (BASE + LEC1);
  tensionbateria = (valor*(4.8/(50000/75000))/255; 
  

}